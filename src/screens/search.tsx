import {Text, View} from 'react-native';

export default function Search() {
  return (
    <View
      style={{
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Movie</Text>
    </View>
  );
}
