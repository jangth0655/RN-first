import {Text, View} from 'react-native';

export default function Movies() {
  return (
    <View
      style={{
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Movies</Text>
    </View>
  );
}
