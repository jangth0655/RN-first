import {Text, View} from 'react-native';

export default function TV() {
  return (
    <View
      style={{
        flex: 1,
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>TV</Text>
    </View>
  );
}
